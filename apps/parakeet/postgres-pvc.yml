apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: parakeet-postgres-pvc
  namespace: galaxy
  labels:
    name: parakeet-postgres-pvc
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
