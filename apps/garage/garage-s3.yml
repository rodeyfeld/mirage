apiVersion: v1
kind: Service
metadata:
  name: garage-s3
  namespace: galaxy
  labels:
    app: garage
spec:
  type: ClusterIP
  selector:
    app: garage
  ports:
  - name: s3-api
    port: 3900
    targetPort: 3900
    protocol: TCP

